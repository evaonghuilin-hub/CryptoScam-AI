# Packages used by the notebooks (01-06) for data processing, modelling,
# experiment tracking, SageMaker Pipeline orchestration and the demo relay.
#
# Installing these lets you open and read the notebooks locally, but the
# notebooks themselves still require a SageMaker Studio environment with an
# attached execution role (sagemaker.get_execution_role()) plus AWS
# credentials for the team's account — they will not run end-to-end on a
# plain local machine. See README.md > Installation.

sagemaker>=2,<3
boto3>=1.34.0
botocore>=1.34.0
kagglehub
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
scipy>=1.11.0
joblib>=1.3.0
mlflow>=2.14.0
matplotlib>=3.8.0
rapidfuzz>=3.14.5

# Notebook 06 only (FastAPI + ngrok relay). Notebook 06 also installs these inline
# with %pip so it is self-contained when run in a fresh SageMaker Studio kernel.
fastapi>=0.110.0
uvicorn>=0.29.0
pyngrok>=7.0.0
requests>=2.31.0
